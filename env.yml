name: niro
channels: [conda-forge, pytorch]
dependencies:
  - python=3.11
  - pytorch=2.3
  - pytorch-cuda=12.1
  - sentencepiece
  - accelerate
  - fastapi
  - uvicorn
  - tqdm
  - pandas
  - pip:
      - flash-attn==2.*          # fused qkv kernels
      - evaluate==0.4.*